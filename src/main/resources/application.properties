# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ecoguard_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=ecoguardSecretKeyForJWTTokenGenerationAndValidation123456789
jwt.expiration=86400000

# Server Configuration
server.port=8080

# Logging
logging.level.com.ecoguard=DEBUG
logging.level.org.springframework.security=DEBUG